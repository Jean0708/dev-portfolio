# Jean Zhou 作品集维护说明

本文件记录当前版本的设计系统、内容结构、源码入口和后续修改规则。当前目标是保留历史创意与现有体验，不重新设计页面，不修改文案、项目数据、链接和交互。

## 维护边界

本轮保留：

- 所有现有中文文案、项目指标、年份、履历时间和项目顺序。
- 首页导航、锚点跳转、案例详情链接、返回链接、PDF 下载/展示链接。
- 首页 IP 动画窗口的拖动、键盘移动、最小化、放大播放、跳过和淡出行为。
- 案例详情页展示窗口的拖动、键盘移动、最小化、最大化、拖拽缩放、键盘缩放、双击恢复尺寸、PDF 内嵌阅读、无 PDF 时图片展示。
- `prefers-reduced-motion`、键盘可访问性和 `aria-label`。
- 当前素材路径，包括 `/assets/*`、`/cases/*`、`/documents/*`、`/og.png`、`/favicon.svg`。

本轮不做：

- 不上线、不改 Sites 或 GitHub Pages。
- 不重构信息架构。
- 不替换素材。
- 不新增文案。
- 不调整项目数据、案例顺序、年份、公司经历或技能条目。

## 当前视觉方向

当前作品集是中文 UX / 交互设计师个人网站，视觉关键词为：

- 温暖治愈的草地场景。
- 文件夹、纸张、复古桌面窗口。
- 深绿色、鼠尾草绿、奶油纸色和荧光黄绿点缀。
- 首页用个人 IP 动画作为开场创意。
- 案例详情页使用深色背景，让 PDF / 图片窗口成为主视觉。

这一版不是最终设计规范，而是“历史样式基线”。后续可以被新的设计规则覆盖，但覆盖前必须先确认哪些交互和内容需要保留。

## 设计 Token

主要 token 在 `app/globals.css` 的 `:root` 中：

```css
--ink: #1e3527;
--forest: #173725;
--sage: #dbe9d6;
--cream: #f5f2e9;
--paper: #fffef9;
--lime: #d8ff65;
--line: rgba(30, 53, 39, 0.28);
--sans: "Inter", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans CJK SC", sans-serif;
```

字体现状：

- `app/fonts.css` 从 Google Fonts 引入 `Instrument Serif` 和 `Inter`。
- 中文默认走 `--sans`，即 Inter + PingFang SC / 微软雅黑 / Noto Sans CJK SC 黑体栈。
- `case.css` 中存在 `var(--serif)` 引用，但当前 `:root` 没有定义 `--serif`。这是现状记录，暂不修。

颜色使用规则：

- 正文和主要线条使用 `--ink`。
- 首页浅色区域使用 `--sage`、`--paper`、`--cream`。
- 作品区使用 `--forest` 深绿背景。
- 互动强调色使用 `--lime`。
- 案例详情页使用 `#0b120d` 深色底，窗口继续使用复古系统灰绿。

## 版式与组件规则

导航：

- 固定在顶部，玻璃感浅纸色背景。
- 桌面显示 WORK / ABOUT / PRACTICE / EXPERIENCE / SKILL / 联系方式。
- 移动端隐藏中间导航，只保留品牌和 CTA。

首页 Hero：

- 背景图为 `/assets/hero-still.png`。
- 初始展示可移动的 `IP-STORY.MP4` 小窗口。
- 点击播放后窗口放大到全屏，顶部标题文案淡入。
- 视频源为 `/assets/ip-running.mp4`，poster 使用 `/assets/hero-still.png`。
- 首页标题和描述目前为固定文案，不要在本轮改动。

纸张区：

- `paper-section` 使用浅纸色 + 细点阵纹理。
- 个人信息卡、工具卡、能力卡使用纸张纹理 `/assets/paper-texture-v2.png`。
- 圆角、描边和阴影都偏“文件夹/纸张”质感，不做现代 SaaS 卡片化替换。

作品卡片：

- 作品区为深绿色背景。
- 案例卡是叠放纸张文件，带 tab、回形针、虚线封面区、编号圆点。
- 移动端变为横向滚动卡片，保留 `scroll-snap`。

案例详情页：

- 页面背景是深色。
- 顶部 `CLOSE ×` 返回 `/#work`。
- PDF / 图片通过可移动、可缩放窗口展示。
- 窗口样式复用旧系统标题栏：深绿标题栏、灰色菜单栏、右上控制按钮、底部状态栏。

## 内容清单

案例数据在 `app/caseData.ts`，当前共 6 个案例：

1. `credit-activity`：Credit 活动，2025，带 PDF。
2. `balance-activity`：Balance 活动，2024，带 PDF。
3. `daily-lazcash`：Daily LazCash 调研助手，2025，无 PDF。
4. `xiaolu-medical`：小麓医疗导诊机器人，2023，带 PDF。
5. `dance-plus`：Dance+ 舞蹈家，2021，带 PDF。
6. `wildsit-game`：Wildsit 科普小游戏，2020，无 PDF。

首页精选展示顺序在 `PortfolioHome.tsx` 的 `homepageCaseSlugs`：

```ts
xiaolu-medical
balance-activity
credit-activity
daily-lazcash
dance-plus
```

能力条目在 `PortfolioHome.tsx` 的 `capabilityRows`：

- 增长策略与体验设计
- UX / 交互设计
- 用户研究与数据分析
- AI 设计实践
- 视觉与动效

履历条目在 `PortfolioHome.tsx` 的 `experiences`：

- 2026.03—2026.05 阿里巴巴 LAZADA
- 2023.07—2025.10 拼多多 TEMU
- 2022.06—2022.10 OPPO
- 2018.08—2019.06 深圳囍汇国际创意

注意：用户提到“简历链接、邮箱、复制邮箱、对话框和浏览模式”需要保留，但当前源码中没有实际邮箱、复制邮箱、简历链接或对话框组件实现。现有代码只包含联系区文案和交互稿中“后续可加入邮箱 / 社交方式”的备注。后续如果新增这些功能，必须单独记录入口、状态和行为，不要混入视觉重构一起做。

## 源码入口

当前已把源码副本整理到本目录，后续优先改这里，不再依赖隐藏缓存路径。

关键文件：

```text
app/PortfolioHome.tsx
首页内容、首页导航、个人信息、作品卡、能力、履历、联系区、首页 IP 动画窗口交互。

app/globals.css
全局视觉 token、首页和各 section 样式、响应式、reveal 动效、prefers-reduced-motion。

app/caseData.ts
案例数据、案例 slug、年份、分类、封面、PDF 路径、summary、tone。

app/work/[slug]/page.tsx
案例详情页框架、案例元信息、返回链接、更多案例。

app/work/[slug]/ResizableDocumentWindow.tsx
案例 PDF / 图片展示窗口，包含拖动、缩放、最大化、最小化、键盘操作。

app/work/[slug]/case.css
案例详情页深色背景与复古窗口视觉。

public/assets/
首页背景、IP 视频、个人照片、纸张纹理。

public/cases/
案例封面图。

public/documents/
案例 PDF。

design/
已有交互稿和参考 SVG。
```

## 后续修改流程

每次修改前先判断任务类型：

1. 只改内容：优先改 `caseData.ts` 或 `PortfolioHome.tsx` 中对应数组，不碰 CSS。
2. 只改视觉：优先改 token 和组件 class，不改文案和数据。
3. 只改素材：保持旧路径或同步更新所有引用，避免图片/PDF 断链。
4. 只改交互：先列出现有状态，再补充新状态，不删除键盘路径和 reduced-motion。
5. 大改首页：先更新本文件，再改代码，防止旧创意被误删。

每次修改后至少检查：

- 首页是否仍能从 hero 滚到 about / work / practice / experience / capabilities / contact。
- 首页 IP 窗口鼠标拖动和键盘方向键移动是否可用。
- 首页播放、跳过、关闭和最小化是否可用。
- 每个首页作品卡是否能跳到 `/work/[slug]`。
- PDF 案例能显示 PDF；无 PDF 案例能显示图片。
- 案例窗口拖动、最大化、最小化、缩放、键盘缩放是否可用。
- 移动端作品卡横向滚动是否仍可用。
- `prefers-reduced-motion` 没有被删除。

## 维护建议

为了减少后续修改成本，建议下一轮先做“结构整理”，但仍不改视觉：

- 把 `capabilityRows`、`experiences` 从 `PortfolioHome.tsx` 拆到独立 `contentData.ts`。
- 把首页动画窗口拆成 `HeroFilmWindow.tsx`。
- 把首页作品卡拆成 `CaseFileCard.tsx`。
- 把颜色和字体 token 补充为完整注释，尤其补上 `--serif`。
- 新增 `docs/content-map.md`，专门记录文案、链接和素材路径。

这样后续每次改文案、改案例、改视觉、改交互都能分开处理，消耗更少上下文和 token。
